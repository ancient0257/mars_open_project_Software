{
  "name": "campus-intel-root",
  "private": true,
  "workspaces": ["frontend", "mcp-servers/*"],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:servers\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:servers": "concurrently \"cd mcp-servers/library && node server.js\" \"cd mcp-servers/cafeteria && node server.js\" \"cd mcp-servers/events && node server.js\" \"cd mcp-servers/academics && node server.js\"",
    "install:all": "npm install && cd frontend && npm install && cd ../mcp-servers/library && npm install && cd ../cafeteria && npm install && cd ../events && npm install && cd ../academics && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
